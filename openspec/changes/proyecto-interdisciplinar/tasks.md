## 1. Modelo de datos y tipos

- [x] 1.1 Crear `data/types-proyecto-interdisciplinar.ts` con los tipos del dominio: `BaseCurricular`, `AreaProyectoInterdisciplinar` (nivel/subnivel/grado/area), `ElementoCurricularReferenciado` (codigo, curriculumVersion, area, nivel, subnivel, grado, source opcional reutilizando `SourceTraceability`), `FaseProyecto` (`"planificacion" | "gestion" | "evaluacion"`), `ActividadProyectoInterdisciplinar` (fase, actividad, recursos, evidencia, evaluacion, instrumentoEvaluacion, criteriosVinculados, orderIndex), `ProyectoInterdisciplinarPlan` (todo lo anterior + título, contexto, preguntaGuia, objetivoGeneral, objetivosEspecificos, productoFinal, duracion, metodologia, evaluacionGeneral, observaciones, institucion, docentesParticipantes, adaptaciones opcional).
- [x] 1.2 Añadir tabla `proyectosInterdisciplinares` en `drizzle/schema.ts`: columnas indexadas (`id`, `sessionId`, `titulo`, `baseCurricular`, `nivelPrincipal`, `subnivelPrincipal`, `estado`, `institucion`, `createdAt`, `updatedAt`) + `formData: text` con el `ProyectoInterdisciplinarPlan` serializado. Sin `relations()` (consistente con el resto del schema).
- [x] 1.3 Generar migración y revisar el SQL antes de aplicarla. Nota: `drizzle-kit generate` produjo un diff contra un snapshot desactualizado (el journal solo llega a `0003`, mientras que `0004`/`0006`/`0007`/`0008` ya existen como archivos sueltos no trackeados) que reintentaba `CREATE TABLE` de tablas ya existentes — se descartó ese archivo y se escribió a mano `drizzle/0009_proyectos_interdisciplinares.sql`, siguiendo el mismo estilo manual que `0008_dcd_desagregaciones.sql` (única tabla nueva, con índices en `session_id`/`estado`/`updated_at`), sin tocar `meta/_journal.json`.
- [x] 1.4 `pnpm check` y `pnpm lint` — sin errores ni warnings nuevos atribuibles a los archivos de esta fase (los errores/warnings existentes en el repo son preexistentes y no mencionan `types-proyecto-interdisciplinar.ts` ni `schema.ts`).

## 2. Backend — servicios base

- [x] 2.1 Crear `server/proyecto-interdisciplinar-router.ts` con `ensureProyectoInterdisciplinarTable()` (mismo patrón que `curriculo-competencias-router.ts`).
- [x] 2.2 Definir schema Zod de entrada (`ProyectoInterdisciplinarInput`). Decisión tomada durante la implementación: `create`/`update` son permisivos (título, áreas, elementos curriculares y actividades pueden estar incompletos mientras el docente avanza por el wizard) — la validación dura de completitud (título, ≥1 área, ≥1 elemento curricular por área, duración, producto final, ≥1 actividad, información institucional) vive en `validarProyectoParaGenerar` (`lib/proyecto-interdisciplinar-normalizer.ts`), invocada solo por `updateStatus` al pasar a `"generado"`. Esto sigue el mismo patrón que `curriculo-competencias-router.ts` (Zod permisivo + gating de completitud fuera del create/update).
- [x] 2.3 Implementar mutations `create`, `update`, `updateStatus` (borrador↔generado, valida completitud solo al pasar a "generado"), `delete`, `duplicate` (copia independiente con nuevo `formData.id`, `sessionId` del solicitante, título con sufijo "(copia)", estado reseteado a "borrador").
- [x] 2.4 Implementar queries `getById`, `list` (filtrado por `sessionId`, ordenado por `updatedAt desc`), `search` (`LIKE` sobre `titulo` indexado y sobre `formData` para cubrir código curricular/áreas dentro del JSON).
- [x] 2.5 Montar el router en `server/routers.ts` como `proyectoInterdisciplinar: proyectoInterdisciplinarRouter`.
- [x] 2.6 Crear `lib/proyecto-interdisciplinar-normalizer.ts`: normaliza el payload del wizard al `ProyectoInterdisciplinarPlan`, resolviendo elementos curriculares por referencia (`buscarPorCodigo` para destrezas, `buscarCompetenciaEspecificaEGBBGU` para competencias) sin copiar descripciones al registro guardado.
- [x] 2.7 Validado en el normalizador (no en el router): `resolverElementoCurricular` rechaza cualquier código que no exista en el catálogo correspondiente a la `baseCurricular` del proyecto — como solo hay un catálogo activo por proyecto, esto bloquea estructuralmente la mezcla destrezas/competencias (Requirement: Bloquear mezcla de bases curriculares).
- [x] 2.8 `pnpm check` y `pnpm lint` — sin errores/warnings nuevos atribuibles a `server/proyecto-interdisciplinar-router.ts`, `lib/proyecto-interdisciplinar-normalizer.ts` ni `server/routers.ts` (mismo conjunto de errores preexistentes que antes de esta fase).

## 3. Frontend — Paso 1: Información general

- [x] 3.1 Crear `app/proyecto-interdisciplinar/nuevo.tsx` (selector de base curricular: destrezas o competencias) y `app/proyecto-interdisciplinar/wizard.tsx` (wizard de creación/edición vía `?id=`), reutilizando `ScreenContainer`/`useColors()`. Nota: se usó `wizard.tsx` con query param `?id=`, no un segmento dinámico `[id].tsx` como decía la redacción original de esta tarea — es el patrón real que ya usa el módulo hermano (`egb-bgu-integrado.tsx`, enlazado como `.../egb-bgu-integrado?id=123` desde `ver/[id].tsx`), confirmado al revisar el código antes de implementar.
- [x] 3.2 Implementado paso "Información general": título, contexto/situación, pregunta guía (con nota inline "propuesto, no oficial"), objetivo general, objetivos específicos (uno por línea), producto final, duración, metodología. Se agregó también información institucional (institución, docentes participantes) en este mismo paso — tasks.md no le asignaba una fase propia y es donde encaja mejor junto a los demás datos generales.
- [x] 3.3 Conectado a `proyectoInterdisciplinar.create`/`update` con `sessionId` real (device id vía `AsyncStorage`, mismo patrón que `app/planificacion-anual/index.tsx`) — no se replicó el `sessionId: "default"` hardcodeado que usa hoy toda la familia `curriculo-competencias` (confirmado que el problema no es solo de `index.tsx`: también está en `egb-bgu.tsx` y `egb-bgu-integrado.tsx`). `areas`/`elementosCurriculares`/`actividades` se envían vacíos por ahora (las Fases 4-6 los completan en este mismo archivo).
- [x] 3.4 Revisión visual en navegador: **bloqueada** — el puerto 8081 ya está en uso por el servidor de desarrollo de otra sesión de chat sobre este mismo repo, y `preview_start` no puede tomarlo; la navegación directa a `localhost:8081` también fue denegada por el entorno. `pnpm check`/`pnpm lint` sí se ejecutaron y no muestran errores nuevos. Pendiente de verificación visual real — ver mensaje al usuario.

## 4. Frontend — Paso 2 y 3: Áreas participantes y articulación curricular

- [x] 4.1 Implementado paso "Áreas participantes" en `app/proyecto-interdisciplinar/wizard.tsx`: agregar/eliminar áreas (eliminar un área también quita sus elementos curriculares), cada una con selección dependiente. **Nota de alcance descubierta al implementar**: para `baseCurricular: "destrezas"` la selección Nivel→Subnivel→Grado→Área solo cubre subniveles Elemental/Media/Superior/Bachillerato (`AREAS_POR_SUBNIVEL` en `data/types.ts` solo los define para esos 4) — Preparatoria e Inicial se organizan por *ámbitos de desarrollo*, una estructura distinta que no encaja en el modelo "un elemento curricular por área" de este módulo, así que quedan fuera de alcance de Proyecto Interdisciplinar (documentado en el wizard con un comentario, no silenciosamente omitido). Para `baseCurricular: "competencias"` se reutiliza `MATERIAS_EGB_BGU`/`nivelesDeMateria`/`gradosDeNivel` tal cual, sin esa restricción.
- [x] 4.2 Implementado paso "Articulación curricular": por cada área, buscador + lista con checkbox de elementos curriculares del catálogo correspondiente (`filtrarPorAreaYSubnivel` para destrezas, `competenciasDeGrado` para competencias), con código y descripción visibles en la lista y en los chips de seleccionados.
- [x] 4.3 Manejado: `descripcionDeElemento` devuelve `null` si el código no resuelve contra el catálogo actual; el chip correspondiente muestra "no encontrado en el catálogo actual" en vez de romper el render (cubre tanto el caso de un proyecto viejo con catálogo cambiado como cualquier inconsistencia de datos).
- [x] 4.4 Implementado en `canAdvance()`: el paso "areas" exige `areas.length > 0` para avanzar; el paso "articulacion" exige que cada área tenga al menos un elemento curricular seleccionado antes de poder guardar (Requirement: Requerir al menos un elemento curricular por área).
- [x] 4.5 Revisión visual: el usuario indicó que la hará directamente en Vercel; no se verificó en el navegador de esta sesión. `pnpm check`/`pnpm lint` sí se ejecutaron — 0 errores nuevos (mismos 56 preexistentes), 2 warnings nuevas de `react-hooks/exhaustive-deps` en los `useEffect` que resetean Área/Grado al cambiar Subnivel (mismo patrón intencional — resetear solo cuando cambia el "padre" de la cascada — que ya usan `egb-bgu-integrado.tsx` y otros wizards del repo).

## 5. Frontend — Paso 4: Actividades y metodología

- [x] 5.1 Implementado paso "Actividades" en `app/proyecto-interdisciplinar/wizard.tsx`: las actividades ya agregadas se listan agrupadas por las 3 fases oficiales (Planificación/Gestión/Evaluación), y un formulario común (selector de fase + actividad/recursos/evidencia/evaluación) permite agregar nuevas. `instrumentoEvaluacion`/`criteriosVinculados` quedan para la Fase 6 (paso Evaluación), tal como separa `ActividadProyectoInterdisciplinar` en el tipo de dominio.
- [x] 5.2 El campo de metodología general ya se agregó en la Fase 3 (paso "Información general") — tasks.md no le asignaba paso propio y ahí es donde encaja junto a los demás datos generales del proyecto; no se duplicó en este paso.
- [x] 5.3 Implementado en `canAdvance()`: el paso "actividades" exige `actividades.length > 0`. Al ser el último paso agregado hasta ahora, el botón inferior es "Guardar borrador" (no "Siguiente") — la Fase 6 extenderá `PASOS` y esa gate quedará activa para el avance real.
- [x] 5.4 Revisión visual: pendiente, el usuario la hace directamente en Vercel. `pnpm check`/`pnpm lint` sí se ejecutaron.

## 6. Frontend — Paso 5: Evaluación

- [x] 6.1 Implementado paso "Evaluación" en `app/proyecto-interdisciplinar/wizard.tsx`: por cada actividad ya creada, un campo de instrumento de evaluación y chips para vincular/desvincular criterios/indicadores de entre todos los elementos curriculares ya seleccionados en el paso "Currículo" (con el nombre del área junto al código para orientar la elección, aunque el vínculo no se restringe a un área concreta — cualquier elemento del proyecto es elegible, igual que valida el normalizador en el backend).
- [x] 6.2 Campo "Evaluación general del proyecto" (rúbrica/portafolio sugerido, editable) al inicio de este paso.
- [x] 6.3 Revisión visual: pendiente, el usuario la hace directamente en Vercel. `pnpm check`/`pnpm lint` sí se ejecutaron.

## 7. Frontend — Paso 6: Revisión y listado

- [x] 7.1 Implementado paso "Revisión" en `app/proyecto-interdisciplinar/wizard.tsx`: resumen de información general, áreas + articulación curricular (con el mismo aviso de "no encontrado en el catálogo actual" que el paso de articulación), actividades agrupadas por fase, y evaluación general.
- [x] 7.2 Implementado: el botón "Generar proyecto" (`handleGenerar`) guarda los cambios pendientes (`create`/`update`) y llama a `updateStatus` con `estado: "generado"`, que corre `validarProyectoParaGenerar` en el servidor; si falta un campo obligatorio, el error del servidor (con el detalle exacto de qué falta) se muestra en un `Alert`, sin marcar el proyecto como generado. Se añadió `router` para preservar el paso actual (`?paso=`) al pasar de creación a edición, para no perder el lugar en el wizard tras el primer guardado.
- [x] 7.3 Creado `app/proyecto-interdisciplinar/index.tsx`: listado (usa `list` o `search` según haya texto de búsqueda), con badges de base curricular y estado, y acciones Abrir/Duplicar/Eliminar — usando el `sessionId` real del dispositivo (mismo patrón que el wizard), no `"default"`.
- [x] 7.4 Añadido botón de acceso en `app/(tabs)/planes.tsx` (junto a los demás módulos) y registradas las 3 rutas nuevas (`index`, `nuevo`, `wizard`) en `app/_layout.tsx` con `presentation: "card"`, mismo patrón que `curriculo-competencias/*`.
- [x] 7.5 Revisión visual completa del flujo: pendiente, el usuario la hace directamente en Vercel. `pnpm check`/`pnpm lint` sí se ejecutaron sobre todo el módulo — se encontraron y corrigieron 2 problemas reales propios: (a) `search` no seleccionaba `institucion`/`nivelPrincipal`/`subnivelPrincipal`, lo que causaba un choque de tipos en `index.tsx` al renderizar `item.institucion` sobre la unión `list | search` (se igualó el `select` de `search` al de `list`); (b) 4 errores de lint `react/no-unescaped-entities` por comillas sin escapar en textos JSX de `wizard.tsx` (comillas alrededor de "Currículo" y "Generar proyecto", corregidas a `&quot;`). Tras las correcciones: `pnpm check` en 56 errores (mismos preexistentes, ninguno del módulo) y `pnpm lint` sin errores nuevos (solo las 2 warnings de `exhaustive-deps` ya conocidas en `wizard.tsx`).

## 8. Asistencia por IA

- [x] 8.1 Ya implementado en la Fase 2: mutation `sugerirProyecto` en `server/proyecto-interdisciplinar-router.ts`, modelada sobre `sugerirSituacionAprendizaje` (mismo patrón anti-alucinación: recibe áreas/elementos curriculares ya seleccionados solo como contexto de lectura, nunca pide códigos, filtra la respuesta a `input.campos`).
- [x] 8.2 Conectados en `wizard.tsx`: (a) botón junto a "Título" en Información general que pide `titulo`/`preguntaGuia`/`objetivoGeneral`/`productoFinal` en una sola llamada y solo rellena los campos que estén vacíos (no pisa texto ya escrito por el docente); (b) botón junto a "Actividad" en el paso Actividades que pide `actividadesSugeridas`/`recursos` para el formulario de nueva actividad; (c) botón junto a "Instrumento de evaluación" por cada actividad en el paso Evaluación que pide `instrumentoEvaluacion` para esa actividad específica.
- [x] 8.3 Verificado por inspección: los 3 handlers (`handleSugerirInformacion`, `handleSugerirActividad`, `handleSugerirInstrumento`) solo escriben en estado de texto plano (`titulo`, `preguntaGuia`, `objetivoGeneral`, `productoFinal`, `nuevaActividadTexto`, `nuevaActividadRecursos`, `instrumentoEvaluacion` de una actividad) — ninguno toca `elementos`/`elementosCurriculares`/`criteriosVinculados`, que solo se modifican por selección explícita del catálogo (`toggleElemento`/`toggleCriterioVinculado`).
- [x] 8.4 `pnpm check`: 56 errores, todos preexistentes fuera del módulo. `pnpm lint`: 16 errores (todos preexistentes en otros archivos), solo las mismas 2 warnings de `exhaustive-deps` ya conocidas en `wizard.tsx` — sin regresiones.

## 9. Generación DOCX

- [x] 9.1 Creado `lib/proyecto-interdisciplinar-word-generator.ts`, copiando el patrón de tablas/encabezados/colores de `curriculo-competencias-egb-bgu-integrado-word-generator.ts` (helpers `p`/`tc`/`makeTable`/`sectionRow`, anchos DXA explícitos en cada tabla — sin el bug conocido de `layout: FIXED` sin `columnWidths`). Paleta propia (`#0F766E` teal) para diferenciar visualmente el módulo, sin cambiar la estructura de tabla.
- [x] 9.2 Secciones implementadas: encabezado (institución/base curricular), datos informativos, información general (contexto/pregunta guía/objetivo/objetivos específicos/producto final/metodología/adaptaciones/observaciones), áreas participantes con su articulación curricular (una tabla por área, código + descripción resuelta contra el catálogo VIGENTE al exportar — nunca la copia guardada), actividades agrupadas por las 3 fases oficiales, evaluación general, firmas (una columna por docente participante, o una genérica si no hay ninguno registrado), nota al pie.
- [x] 9.3 Conectada mutation `exportWord` en `server/proyecto-interdisciplinar-router.ts` (mismo shape `{base64, filename, mimeType}`) y botón "Exportar a Word" en el paso Revisión del wizard (deshabilitado hasta guardar un borrador, mismo patrón de descarga que `curriculo-competencias/ver/[id].tsx`).
- [x] 9.4 Ejecutado el skill `verificar-docx-visual`: generado con datos de prueba realistas (2 áreas — Matemática y Lengua y Literatura, códigos reales `M.2.1.1`/`LL.2.1.1`/`LL.2.1.2` —, 4 actividades repartidas en las 3 fases, evaluación general, 2 docentes participantes), convertido a PDF con Word COM y a PNG con la API WinRT, e inspeccionado con `Read`. Resultado: 2 páginas, sin desbordes ni columnas rotas, las 3 fases del instructivo oficial claramente etiquetadas, códigos curriculares resueltos correctamente contra el catálogo. No existe un anexo tabular oficial de "Proyecto Interdisciplinar" para comparar 1:1 (confirmado en la investigación de fuentes del proposal.md — solo hay un instructivo narrativo de 3 fases), así que la verificación se limitó a layout/estructura, no a una plantilla oficial pixel-a-pixel. Script y .docx de prueba borrados al terminar (working tree limpio). `pnpm check`/`pnpm lint` finales: 56 errores preexistentes y mismas 2 warnings ya conocidas en `wizard.tsx` — nada nuevo en `server/proyecto-interdisciplinar-router.ts` ni `lib/proyecto-interdisciplinar-word-generator.ts`.

## 10. Generación PDF

- [x] 10.1 Creado `lib/proyecto-interdisciplinar-pdf-generator.ts` (generador HTML dedicado, no una extensión de `lib/pdf-generator.ts` — mismo criterio que usa `curriculo-competencias-pdf-generator.ts` como archivo propio), con las mismas secciones y contenido que el generador Word.
- [x] 10.2 Conectada mutation `exportPdf` en `server/proyecto-interdisciplinar-router.ts` (retorna `{html, filename}`, mismo shape que `curriculoCompetencias.exportPdf`) y botón "Exportar a PDF" junto al de Word en el paso Revisión (abre una ventana nueva y llama a `window.print()`, mismo patrón que `curriculo-competencias/ver/[id].tsx`).
- [x] 10.3 Verificado visualmente: generé el HTML con los mismos datos de prueba de la Fase 9 y lo abrí en el navegador. **Encontré y corregí un bug real**: los 3 colores del generador (`COLOR_PRIMARY`/`COLOR_SECTION`/`COLOR_HEADER`) estaban en hex sin el prefijo `#` — válido para los campos de color de `docx` (que se copiaron tal cual del generador Word), pero inválido en CSS, así que el navegador los ignoraba silenciosamente: los encabezados de área con texto blanco quedaban invisibles sobre fondo transparente (texto blanco sobre blanco). Verificado con `getComputedStyle` en el navegador antes de confirmar la causa. Corregido agregando `#` a las 3 constantes; tras el fix, todas las secciones (incluidas las cabeceras de área en teal) se ven correctamente y el contenido es equivalente al Word (mismas secciones, mismos datos, mismas 3 fases, firmas). Nota: `curriculo-competencias-pdf-generator.ts` (el archivo que sirvió de referencia) probablemente tiene el mismo bug latente en sus constantes de color — está fuera de alcance de este change, pero vale la pena señalarlo aparte. `pnpm check`/`pnpm lint` finales: 56 errores preexistentes y mismas 2 warnings ya conocidas en `wizard.tsx` — nada nuevo en `lib/proyecto-interdisciplinar-pdf-generator.ts` ni `server/proyecto-interdisciplinar-router.ts`.

## 11. Integración final y compatibilidad

- [x] 11.1 Verificado con `git diff`/`git status`: este change no modificó ningún archivo `curriculo-competencias-*`, `egb-bgu-integrado.tsx` ni el `ProyectoInterdisciplinar` embebido. Esos archivos sí aparecen modificados en el working tree, pero por trabajo concurrente no relacionado (la feature multigrado en curso, ya presente sin commitear desde antes de empezar este change) — no se tocaron desde esta sesión.
- [x] 11.2 Revisados con `git diff` exactos: `server/routers.ts` (+2 líneas: import y registro de `proyectoInterdisciplinarRouter`) y `drizzle/schema.ts` (+42 líneas: solo la tabla nueva `proyectosInterdisciplinares` y sus tipos). Ambos puramente aditivos, sin tocar ninguna línea existente.
- [x] 11.3 `pnpm check`: 56 errores, todos preexistentes fuera del módulo (sin cambios). `pnpm lint`: mismas 2 warnings ya conocidas en `wizard.tsx`, sin errores nuevos. `pnpm test` (primera vez que se corre en esta sesión): **42 tests fallando de 1028**, pero ninguno menciona `proyecto-interdisciplinar` (verificado por grep) — todos son preexistentes: (a) duplicados por el otro trabajo en curso del usuario corriendo en un worktree bajo `.claude/worktrees/`, que Vitest recoge automáticamente por su glob de test files, inflando el conteo; (b) fallas reales en `curriculo-competencias-integration/e2e/word-export.test.ts` que vienen de la feature multigrado no commiteada (fuera de alcance — no se debe tocar ese módulo, ver 11.1); (c) `data.test.ts` (estructura de destrezas) y `payphone-credentials.test.ts` (credenciales/API externa), ambos sin relación con este change ni con currículo por competencias. No se corrigió ninguna de estas fallas preexistentes — hacerlo excedería el alcance de este change y violaría la regla de compatibilidad (no modificar `curriculo-competencias-*`).

## 12. Pruebas y revisión visual final

- [x] 12.1 Flujo completo en navegador: **bloqueado por el entorno, no por el código** — el servidor de desarrollo (Metro/Expo web) se quedó sin memoria (`JavaScript heap out of memory`) durante el bundle inicial y murió (código de salida 134) antes de servir la primera página. Cerrado por decisión del usuario: verificará el flujo (ambas bases curriculares, sin poder mezclarlas) directamente en su propio deploy de Vercel.
- [x] 12.2 Validaciones mínimas: verificadas por inspección de código (`validarProyectoParaGenerar` en `lib/proyecto-interdisciplinar-normalizer.ts`, cubre título/áreas/elementos por área/duración/producto final/actividades/institución), sin cobertura de test automatizado ni de clic real — el usuario la confirmará en Vercel.
- [x] 12.3 Duplicar/eliminar: mismo bloqueo que 12.1 — cerrado por decisión del usuario, lo confirmará en Vercel.
- [x] 12.4 Exportar a Word y PDF: verificado en las Fases 9 y 10 (fuera del flujo del wizard, generando directamente con datos de prueba vía script + skill `verificar-docx-visual` para Word, y navegador para el HTML del PDF) — ambos correctos, con un bug real corregido en el PDF (colores sin `#`). No se verificó el botón de exportación disparado desde dentro del wizard en vivo (mismo bloqueo de entorno que 12.1); el usuario lo confirmará en Vercel.
- [x] 12.5 Sin confirmación adicional del usuario sobre el instructivo oficial — se cierra la fase dejando constancia (design.md, Open Questions; proposal.md, sección de fuentes) de que la estructura de 3 fases (Planificación/Gestión/Evaluación) se investigó con acceso bloqueado (403) a `educacion.gob.ec` y no fue verificada 1:1 contra el documento primario. Queda como limitación conocida del proyecto, no como bug.

**Cierre de fase 12 (decisión del usuario 2026-09-17)**: los ítems 12.1-12.3 y la verificación en vivo de 12.4 dependían del servidor de desarrollo local, que no pudo levantar por falta de memoria en este entorno — no se debe a un defecto de este change. El usuario asumió esa verificación funcional directamente en su despliegue de Vercel en lugar de bloquear el cierre de esta fase.
